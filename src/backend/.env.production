# Project Alpine Backend - Production Configuration

# Server port (internal)
PORT=3001

# Environment mode
NODE_ENV=production

# Frontend URL for CORS (update with your domain)
FRONTEND_URL=https://your-domain.com

# JWT Secret - CHANGE THIS IN PRODUCTION!
# Generate with: openssl rand -base64 48
JWT_SECRET=your-secure-jwt-secret-change-this-in-production

# Database path (uses default ./data/ directory)
